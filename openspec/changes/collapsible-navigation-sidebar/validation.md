# Validation — 2026-09-12

Implemented the shared left navigation collapse/expand control in `apps/web/src/main.jsx` and `apps/web/src/style.css`. Desktop collapses to a 64px rail; mobile keeps the brand and toggle in a compact header. The sidebar starts expanded after a page load and retains its state during in-page navigation and viewport changes.

## Automated checks

- `npm run build` — passed, production assets built successfully.
- `npm run check` — passed, including ESLint and Prettier.
- `ROLECAST_TEST_PORT=3192 npx playwright test tests/browser/app.spec.js tests/browser/voice.spec.js tests/browser/marketplace.spec.js tests/browser/stage.spec.js` — 27 passed in 1.1 minutes.
- `openspec validate --all --strict` — 17 passed, 0 failed.
- `git diff --check` — passed.

The browser fixture ran on port 3192 because port 3100 was already occupied. Launching the fixture and Chromium required execution outside the filesystem/network sandbox. Browser history checks create their own completed drill so they work with workspace isolation and do not depend on another test's seeded records.

## Covered behavior

- Enter/Space activation, visible keyboard focus retained on the toggle, updated Traditional Chinese names and `aria-expanded`, and the `aria-controls` association.
- Hidden navigation is excluded from accessible role queries and Tab navigation; the expand button remains reachable.
- Collapsing increases desktop main-content width and reduces mobile header height; desktop history still scrolls at a 450px viewport height.
- Current history selection survives toggling and reload; the sidebar returns to expanded after reload.
- Home background, selected plot, and unsaved editor fields survive toggling. Entering the marketplace introduction through the main content keeps the sidebar collapsed.
- Mobile history opened before collapse is restored after expansion; selecting a record retains the existing behavior of closing the mobile history list.
- Both sidebar states have no horizontal overflow at 320, 390, 700, 701, 768, 1000, 1024, and 1440px. The expand control remains visible with a 44px hit area, and collapse persists while crossing responsive breakpoints. The responsive test runs with reduced motion; no layout animation was introduced.
- Desktop and mobile voice toggles preserve drill/call identity, provider connection and greeting counts, microphone request count, live media tracks, ongoing capture/playback, and existing captions. Existing ATM, stage, marketplace, editor, recovery, and call-lifecycle checks also pass.

## Visual review

Reviewed expanded/collapsed desktop and mobile screenshots generated by the sidebar tests. The expanded sidebar retains the orange/cream theme, the desktop rail exposes more workspace, and the compact mobile header retains the brand and expand control without clipping.

Screenshots from the final browser run:

- `test-results/app-sidebar-toggle-preserv-ad59e-avigation-and-desktop-space/sidebar-desktop-expanded.png`
- `test-results/app-sidebar-toggle-preserv-ad59e-avigation-and-desktop-space/sidebar-desktop-collapsed.png`
- `test-results/app-sidebar-mobile-history-411e2-survive-collapse-and-resize/sidebar-mobile-expanded.png`
- `test-results/app-sidebar-mobile-history-411e2-survive-collapse-and-resize/sidebar-mobile-collapsed.png`

## Scope of evidence

Browser checks use Chromium, the existing fixture agents/provider, and fake microphone media. They do not claim real-device microphone, paid-provider, or manual screen-reader validation. No backend behavior or dependencies changed; backend unit suites were not rerun for this frontend change.

All six implementation tasks are complete. The change remains active for review and later specification synchronization/archive.
