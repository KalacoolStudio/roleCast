## 1. Command Entry Point

- [x] 1.1 Add the root justfile with a private default list recipe and documented status recipe; verify `just`, `just --list`, and `just status` succeed and correctly describe the documentation-only project.

## 2. Usage Documentation

- [x] 2.1 Add README.md with the just prerequisite, installation reference, available commands, and project status; verify every documented project command works and local file links resolve.
