# Drill live stage validation

Verified on 2026-09-12 using Node.js 26.7.0 and the repository lockfile.

## Automated checks

- `just check`: ESLint and Prettier passed.
- `just test`: production Vite build passed; **52 Vitest tests across 6 files** and **10 Playwright tests** passed.
- `git diff --check`: passed.
- `openspec validate drill-live-stage --strict`: passed.

Coverage includes original end conditions (user, Persona, Judge, turn/call limits), finish during all lifecycle states, cancelled/late model jobs, persistent persona identity and private-context isolation. New checks cover atomic state/event rollback, sequence/revision ordering, v1/v2 migration to v3, recovery without model calls, event pagination and invalid cursors, real HTTP SSE catch-up/reconnect, bounded slow-client handling and subscriber cleanup, presentation cancellation and catch-up, reduced motion, mobile composition, legacy snapshots, and SSE-to-polling fallback.

## Visual review

Reviewed all 40 source cells as displayed, including the five idle appearances and walk cycles. Per-frame foot offsets were measured from source pixels; nested clipping prevents adjacent-row feet from leaking into a shifted frame. Rendering brightness and multiply blending remove the pale paper field on the current light stage. Original PNG bytes are unchanged; no new raster images or image dependencies were required.

Reviewed the real UI in Chromium at 1440×1000 and 390×844. The stage is beside the conversation on desktop, above it on mobile, and collapses to a named role/status summary during mobile composition. Labels and controls remain readable; no horizontal overflow. Screenshots are generated by `just test` (ignored build artifacts):

- `test-results/stage-live-stage-follows-r-ea18a-and-restores-without-replay/stage-desktop-awaiting.png`
- `test-results/stage-live-stage-follows-r-ea18a-and-restores-without-replay/stage-desktop-call.png`
- `test-results/stage-mobile-reduced-motio-ec4d6-t-role-summary-while-typing/stage-mobile.png`
- `test-results/stage-mobile-reduced-motio-ec4d6-t-role-summary-while-typing/stage-mobile-typing.png`
- Additional local contact sheet: `.tmp/stage/sprite-frames.png`.

## Production entry point

Started the built app through `just start` on isolated port 3301 and a temporary fixture database. Explicit fixture-only LLM configuration prevented use of the user's credentials. Verified the production HTML, health endpoint, all five PNG URLs (matching original file hashes), saved SSE events and the terminal public snapshot. Stopped the temporary server afterward.

No paid LLM calls, public tunnels or deployments were made. The user's `.env` and actual application database were not changed. README documents the existing `just dev`, `just build`, `just start` and `just tunnel` workflows, manual same-origin tunnel checks, v3 migration and backup-based rollback. A live ngrok tunnel was not tested; local same-host request checks and stream behavior are covered independently.

## Scope

All 15 implementation tasks are complete, including the sprite-motion follow-up. The stage uses plain zones and fixed paths. Office backgrounds, free navigation, multiplayer, full historical animation replay and a Reporter sprite remain outside this change. Existing uncommitted origin-handling edits in `app.js` and their runtime tests were preserved. This change has not been committed, pushed or archived.

## Sprite-motion follow-up (2026-09-12)

Fixed walking sprites that were tied to a lifecycle flag instead of actual position changes. The character component now plays the manifest walk frames 4/5/6/7 while its position changes, uses the scheduler's exact transition duration, mirrors leftward movement, and returns to idle frame 0 on arrival. Restoration and reduced motion use zero-duration placement.

After this fix, the production build, 10 stage Vitest tests, all 5 stage Playwright tests and just check passed. A new browser test samples rendered positions, crop offsets and animation frames through Persona arrival/departure and Judge monitoring/return, confirms all four walk frames while moving in both directions, and confirms idle on arrival. Samples are attached to the test result; the walking screenshot is test-results/stage-walk-sprites-change--cc797-ly-move-then-return-to-idle/judge-walking.png. The earlier full-suite run remains recorded above; only the affected stage suite was rerun for this frontend correction.
