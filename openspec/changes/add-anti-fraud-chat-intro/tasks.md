## 1. Introduction and handoff

- [x] 1.1 Add the prepared Lin chat, reference photos, order dialog, reveal/replay controls, and verification result; verify exact opening text, working keyboard controls, image loading, reduced motion, and mobile layout in the browser.
- [x] 1.2 Route only the built-in anti-fraud home start through the intro and connect the callback to existing drill creation; verify background preservation, error/conflict handling, navigation cleanup, and no scripted transcript evidence or premature creation.

## 2. Verification and published specs

- [x] 2.1 Add focused intro browser tests and adapt existing call-flow setup; verify the production build, complete browser suite, lint/format, and desktop/mobile screenshots.
- [x] 2.2 Sync the interface delta, update the OpenSpec index, and record results and scope; verify strict OpenSpec validation and clean diff checks.

## 3. Main-branch integration

- [ ] 3.1 Merge current main into style, preserving both the prepared chat/style and incoming voice-only calls, workspaces, ATM, and deployment behavior; reconcile code/tests and OpenSpec contracts, then verify build, unit/integration/browser tests, lint/format, strict specs, and clean diff checks before recording merge validation.
